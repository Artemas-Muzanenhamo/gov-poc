spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: license-service-db
server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka
