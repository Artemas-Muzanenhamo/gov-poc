spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://configuration-server:8888
      fail-fast: true
  config:
    activate:
      on-profile: docker

---

spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  config:
    activate:
      on-profile: dev
